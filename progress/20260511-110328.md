# 2026-05-11 11:03:28 进展记录

## 本次目标

实现 Phase 1 的第一步：PostgreSQL migration、迁移命令和 API 数据库健康检查。

## 已完成

1. 新增初始数据库迁移。
   - `services/api/migrations/000001_initial_schema.up.sql`
   - `services/api/migrations/000001_initial_schema.down.sql`
   - 覆盖 `users`、`refresh_tokens`、`documents`、`document_contents`。
   - 启用 `pgcrypto`，使用 `gen_random_uuid()` 生成 UUID。
   - 新增 `set_updated_at()` 触发器函数和核心索引。

2. 实现轻量迁移执行器。
   - 新增 `internal/database/migrations.go`。
   - 使用 `schema_migrations` 表记录已执行迁移。
   - 支持按文件名顺序执行 `*.up.sql`。
   - 每个迁移文件在独立事务中执行。

3. 完成 `cmd/migrate` 命令。
   - 默认执行 `up`。
   - 默认查找 `services/api/migrations` 或 `migrations` 目录。
   - 启动时读取 `.env` 和 `DATABASE_URL`。
   - 可通过 `-dir` 和 `-timeout` 调整迁移目录和超时时间。

4. 接入 API 数据库连接。
   - API 启动时读取 `DATABASE_URL`。
   - 启动阶段执行数据库 ping，失败则 fail fast。
   - `/health` 现在会检查数据库连通性。

5. 新增快捷命令。
   - `pnpm migrate:api`
   - `make migrate-api`

## 验证结果

1. `gofmt -w ./services/api` 通过。
2. `go test ./services/api/...` 通过。
3. `pnpm build:api` 通过。
4. IDE diagnostics 无新增错误。

## 补充验证结果

2026-05-12 已在真实 Docker PostgreSQL 容器环境中补跑端到端 migration 验证，验证通过。

执行命令：

```bash
docker compose up -d postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/my_notion_go?sslmode=disable pnpm migrate:api
DATABASE_URL=postgres://postgres:postgres@localhost:5432/my_notion_go?sslmode=disable pnpm dev:api
```

数据库验证结果：

1. `postgres` 容器已正常运行。
2. `\dt` 确认已创建 `users`、`refresh_tokens`、`documents`、`document_contents`、`schema_migrations` 五张表。
3. `schema_migrations` 中已记录 `000001_initial_schema`，说明初始迁移已成功执行。
4. 再次执行 `pnpm migrate:api` 输出 `database schema is already up to date`，说明迁移命令具备幂等性。
5. 启动 `pnpm dev:api` 后，Go API 成功监听 `:8080`。
6. 访问 `/health` 返回 `200`，说明 API 数据库连接和健康检查通过。

## 下一步建议

1. 实现统一响应结构和业务错误码。
2. 开始 Auth MVP：注册、登录、JWT、Refresh Token、`GET /api/v1/me`。
3. 在 Auth 后端完成后，更新 `services/api/docs/openapi.yaml`。
