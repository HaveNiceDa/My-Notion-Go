# 2026-05-14 22:09:20 进展记录

## 本次目标

补充 Auth API 调试文件，并在真实 PostgreSQL 环境下手动验证 Auth 主链路。

## 已完成

1. 新增 Auth API 调试文件。
   - 新增 `services/api/docs/auth.http`。
   - 覆盖 `GET /health`、`POST /api/v1/auth/register`、`POST /api/v1/auth/login`、`GET /api/v1/me`、`POST /api/v1/auth/refresh`、`POST /api/v1/auth/logout`。
   - 使用 `.http` 变量统一维护 `baseUrl`、测试邮箱、密码、用户名和设备名。
   - `GET /api/v1/me`、`refresh`、`logout` 会引用 `login` 响应里的 `accessToken` 和 `refreshToken`。

2. 启动真实 PostgreSQL 和 API 服务。
   - `docker compose up -d postgres` 确认 PostgreSQL 容器运行。
   - `DATABASE_URL=postgres://postgres:postgres@localhost:5432/my_notion_go?sslmode=disable pnpm migrate:api` 返回 `database schema is already up to date`。
   - `DATABASE_URL=postgres://postgres:postgres@localhost:5432/my_notion_go?sslmode=disable pnpm dev:api` 成功启动 Go API。
   - API 成功监听 `:8080`。

3. 手动验证 Auth 主链路。
   - `GET /health` 返回 `200`，说明 API 和数据库健康检查正常。
   - `POST /api/v1/auth/register` 返回 `201`，说明用户注册成功。
   - `POST /api/v1/auth/login` 返回 `200`，说明邮箱密码登录成功。
   - `GET /api/v1/me` 返回 `200`，说明 Access Token 鉴权和当前用户接口正常。

4. 补测 Refresh Token 轮换和退出登录。
   - 测试邮箱：`auth-refresh-logout-1778767866@example.com`。
   - `POST /api/v1/auth/refresh` 使用登录返回的 refresh token，返回 `200`，说明可以续期登录态。
   - 使用刷新后的 access token 请求 `GET /api/v1/me`，返回 `200`，说明新 access token 可用。
   - 再次使用旧的登录 refresh token 请求 `POST /api/v1/auth/refresh`，返回 `401 INVALID_TOKEN`，说明 refresh token 轮换后旧 token 已失效。
   - 使用刷新后的 refresh token 请求 `POST /api/v1/auth/logout`，返回 `200`，说明退出登录可以撤销 refresh token。
   - logout 后再次使用同一个 refresh token 请求 `POST /api/v1/auth/refresh`，返回 `401 INVALID_TOKEN`，说明被撤销的 token 无法继续续期。

5. 处理 Gin trusted proxies 安全提示。
   - 更新 `services/api/cmd/api/main.go`。
   - 在 `gin.New()` 后显式调用 `router.SetTrustedProxies(nil)`。
   - 本地开发不经过反向代理，因此不信任任何代理来源，符合最小权限原则。
   - 短启动日志中不再出现 `trusted all proxies` warning。

## 验证结果

1. Auth API 的注册、登录和当前用户读取链路已经跑通。
2. PostgreSQL migration 具备幂等性，重复执行不会重复建表。
3. Gin 路由注册正常，Auth 相关路由均已挂载到 `/api/v1`。
4. 注册时出现的 GORM `record not found` 日志属于预期行为，用于判断邮箱尚未被注册，不影响接口成功返回。
5. Refresh token 轮换、旧 token 失效、logout 撤销 token 均已通过真实接口验证。
6. `gofmt -w ./services/api/cmd/api/main.go` 通过。
7. `go test ./services/api/...` 通过。
8. API 短启动验证确认 `trusted all proxies` warning 已消失。

## 已知提示

1. `auth.http` 文件在 Trae 中需要 REST Client 类插件才会显示 `Send Request` 按钮。
   - 如果没有插件，也可以继续使用终端 `curl` 方式验证接口。

## 下一步建议

1. 开始前端 Auth 页面和登录态接入。
   - 登录页。
   - 注册页。
   - token 存储。
   - `/api/v1/me` 拉取当前用户。
   - 基础路由保护。
2. 前端 Auth 打通后，进入 Document CRUD 后端模块。
