# 2026-05-14 23:35:17 进展记录

## 本次目标

修复 Web 端 `http://localhost:5273` 调用 Go API `http://localhost:8080` 时的 CORS 预检失败问题。

## 已完成

1. 接入成熟 CORS 中间件。
   - 使用 `github.com/gin-contrib/cors`。
   - 该依赖此前已在 `go.mod` 中作为 indirect 存在，本次在 `cmd/api/main.go` 正式 import 后转为直接依赖。

2. 在 Gin 启动链路注册 CORS。
   - 更新 `services/api/cmd/api/main.go`。
   - 在业务路由注册之前执行 `router.Use(cors.New(...))`。
   - 让浏览器 `OPTIONS` preflight 请求能先拿到跨域响应头。

3. 使用最小权限的本地默认白名单。
   - 更新 `services/api/internal/config/config.go`。
   - 新增 `CORSAllowedOrigins []string`。
   - 默认只允许 `http://localhost:5273`。
   - 支持通过 `CORS_ALLOWED_ORIGINS` 配置逗号分隔的多个 origin。

4. 更新环境变量示例。
   - 更新 `.env.example`。
   - 新增 `CORS_ALLOWED_ORIGINS=http://localhost:5273`。

## 验证结果

1. `gofmt -w ./services/api/cmd/api/main.go ./services/api/internal/config/config.go` 通过。
2. `go mod tidy` 通过。
3. `go test ./services/api/...` 通过。
4. IDE diagnostics 无新增错误。

## 已知提示

1. 如果当前本地 API 服务还在运行旧代码，需要重启 `pnpm dev:api` 后 CORS 配置才会生效。
2. 当前默认只放行 Web 开发端口 `5273`，符合最小权限原则。
3. 如果后续有其它本地前端端口或部署域名，需要通过 `CORS_ALLOWED_ORIGINS` 显式加入。

## 下一步建议

1. 重启 Go API 服务。
2. 在浏览器重新发起注册请求，确认 preflight 不再报 `No Access-Control-Allow-Origin`。
3. Auth 前端 CORS 联调通过后，继续验证注册、登录、刷新页面恢复登录态和退出登录。
