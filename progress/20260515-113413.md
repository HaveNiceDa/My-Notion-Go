# 2026-05-15 11:34:13 进展记录

## 本次目标

将前端首页、登录注册和 Document MVP 对齐原 My-Notion 风格，并补充主题切换能力。

## 已完成

1. 建立视觉对齐约束。
   - 新增 `.trae/skills/my-notion-visual-alignment/SKILL.md`。
   - 明确 `my-notion-go/apps/web` UI 必须参考原项目 `/Users/bytedance/Desktop/project/My-Notion/apps/web`。
   - 明确 Document 工作区不能继续使用通用 SaaS dashboard 卡片风格。

2. 参考原 My-Notion Web 视觉。
   - 参考原营销页：
     - `apps/web/src/app/[locale]/(marketing)/_components/Navbar.tsx`
     - `apps/web/src/app/[locale]/(marketing)/_components/Heading.tsx`
     - `apps/web/src/app/[locale]/(marketing)/_components/Heroes.tsx`
   - 参考原工作区：
     - `apps/web/src/app/[locale]/(main)/_components/Navigation.tsx`
     - `apps/web/src/app/[locale]/(main)/_components/Item.tsx`
     - `apps/web/src/app/[locale]/(main)/_components/document-list.tsx`
     - `apps/web/src/app/[locale]/(main)/_components/Navbar.tsx`
     - `apps/web/src/app/[locale]/(main)/_components/Title.tsx`
     - `apps/web/src/app/[locale]/(main)/(routes)/documents/page.tsx`

3. 复用原项目静态资产。
   - 新增 `apps/web/public/`。
   - 复制原项目资产：
     - `logo.svg`
     - `logo-dark.svg`
     - `documents.png`
     - `documents-dark.png`
     - `empty.png`
     - `empty-dark.png`
     - `reading.png`
     - `reading-dark.png`

4. 首页样式对齐原 My-Notion。
   - 更新 `apps/web/src/features/auth/AuthShell.tsx`。
   - 将默认首页改成 marketing landing page。
   - 顶部包含 logo、登录、注册 CTA 和主题切换。
   - Hero 使用原项目 documents / reading 静态图片。
   - 登录注册入口改为类似 Clerk `SignInButton mode="modal"` 的弹窗形式。

5. 登录注册改为弹窗形式。
   - 保留原有 Go Auth API 调用逻辑。
   - 保留 React Hook Form + Zod 校验。
   - 保留 ahooks `useRequest` 管理请求生命周期。
   - `/login` 和 `/register` 仍可直接访问，但呈现为首页上的 auth modal。

6. 实现主题切换。
   - 新增 `apps/web/src/features/theme/themeStore.ts`。
   - 使用 Zustand 管理 `light` / `dark`。
   - 使用 `localStorage` 持久化主题。
   - 通过 `document.documentElement.classList` 切换 `.dark` class。
   - 首页和工作区均接入主题切换按钮。

7. 扩展 API client 的 Document 能力。
   - 更新 `packages/api-client/src/index.ts`。
   - 新增类型：
     - `Document`
     - `DocumentTreeNode`
     - `CreateDocumentRequest`
     - `UpdateDocumentRequest`
   - 新增 `documentApi`：
     - `create`
     - `tree`
     - `get`
     - `update`
     - `archive`

8. 实现前端 Document MVP。
   - 新增 `apps/web/src/features/documents/DocumentWorkspace.tsx`。
   - 支持 Dashboard / Document 工作区壳。
   - 支持左侧文档树。
   - 支持新建根文档。
   - 支持新建子文档。
   - 支持点击文档进入 `/documents/:documentId`。
   - 支持文档详情页展示标题和更新时间。
   - 支持内联标题编辑并调用 `PATCH /api/v1/documents/:id`。
   - 支持归档当前文档并调用 `POST /api/v1/documents/:id/archive`。
   - 支持空状态页使用原项目 `empty.png` / `empty-dark.png`。

9. 重写全局样式。
   - 更新 `apps/web/src/styles/global.css`。
   - 使用原 My-Notion 的系统字体栈。
   - 使用黑白灰 CSS variables。
   - 实现 light / dark 主题变量。
   - 实现 marketing 首页、auth modal、sidebar、document tree、document detail、empty state 样式。

10. 补充依赖。
    - 为 `@my-notion-go/web` 添加 `lucide-react`，用于对齐原项目图标风格。
    - 暂未安装完整 shadcn / Tailwind；本次采用 shadcn/Radix 风格的本地 CSS 与组件结构，避免一次性引入过多基建。
    - 暂未安装 Playwright；当前先用源码和构建验证，后续需要真实渲染截图对比时再引入。

## 验证结果

1. `pnpm typecheck` 通过。
2. `pnpm build:web` 通过。
3. IDE diagnostics 无 TypeScript 编译错误。
4. IDE diagnostics 仅有 cSpell 对 `ahooks`、`topbar` 等技术词的提示。

## 已知限制

1. 本次未做富文本编辑器，只完成 Document metadata 层的标题编辑。
2. 本次未做搜索、设置弹窗、回收站弹窗和拖拽排序。
3. 本次未接入完整 shadcn CLI / Tailwind，后续如需要标准 shadcn 组件库，可单独做 UI 基建迁移。
4. 本次未做 Playwright 视觉截图验证，后续如果页面细节仍需对齐，可启动浏览器对比原项目截图。

## 下一步建议

1. 启动 `pnpm dev:api` 和 `pnpm dev:web` 做浏览器手动联调。
2. 验证首页主题切换、登录弹窗、注册弹窗和进入工作区。
3. 验证文档树、新建根文档、新建子文档、点击文档、标题编辑和归档。
4. 根据浏览器实际效果微调 spacing、hover、sidebar 宽度和空状态图片尺寸。
5. 后续进入 Editor MVP：接入富文本编辑器和 `document_contents` JSONB 保存。
