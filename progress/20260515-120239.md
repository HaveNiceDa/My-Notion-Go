# 2026-05-15 12:02:39 进展记录

## 本次目标

根据代码组织反馈，补充单文件单一职责规范，并重构前端 Auth / Document MVP 文件结构，同时为前端业务组件补充中文学习型注释。

## 已完成

1. 更新项目 Skills。
   - 更新 `.trae/skills/my-notion-go-rewrite/SKILL.md`。
   - 新增 `File Responsibility Rule`。
   - 明确前端应拆分 route shell、auth dialog、marketing page、workspace sidebar、tree row、detail view、empty state 等职责。
   - 明确后端应继续保持 model / repository / service / handler / middleware 分层文件。
   - 明确新增业务注释优先使用中文学习型注释。

2. 更新 Web Hooks Skill。
   - 更新 `.trae/skills/my-notion-go-web-hooks/SKILL.md`。
   - 明确前端组件文件应尽量单一职责。
   - 明确业务 React 组件、feature container、请求 hook、路由守卫等需要中文说明。

3. 更新视觉对齐 Skill。
   - 更新 `.trae/skills/my-notion-visual-alignment/SKILL.md`。
   - 明确视觉组件也要按职责拆分，避免 sidebar、tree row、navbar、modal、empty state、detail surface 全部堆在一个文件。
   - 明确中文注释应说明组件对齐了原 My-Notion 的哪个模式。

4. 拆分 Auth 前端文件。
   - `AuthShell.tsx` 现在只负责路由装配。
   - 新增 `AuthBootstrap.tsx`，负责启动时恢复主题和登录态。
   - 新增 `routeGuards.tsx`，负责 `PublicOnlyRoute` 和 `ProtectedRoute`。
   - 新增 `MarketingHome.tsx`，负责原 My-Notion 风格首页。
   - 新增 `AuthDialog.tsx`，负责 Clerk 风格登录/注册弹窗。
   - 新增 `AuthenticatedWorkspace.tsx`，负责把 logout 流程注入 Document 工作区。
   - 新增 `LoadingScreen.tsx`，负责启动 loading 状态。
   - 新增 `authSchemas.ts`、`authErrors.ts`、`types.ts`，分别承载表单 schema、错误转换和类型。

5. 拆分 Document 前端文件。
   - `DocumentWorkspace.tsx` 现在只负责 Document MVP 容器层：React Query、mutation、路由跳转和状态编排。
   - 新增 `WorkspaceSidebar.tsx`，负责左侧用户区、入口、文档树和底部操作。
   - 新增 `DocumentTree.tsx`，负责根文档树渲染。
   - 新增 `DocumentTreeItem.tsx`，负责递归树行、展开折叠和新建子页。
   - 新增 `DocumentNavbar.tsx`，负责顶部栏、主题切换和归档入口。
   - 新增 `DocumentNavbarTitle.tsx`，负责顶部标题区域。
   - 新增 `DocumentDetail.tsx`，负责文档详情 MVP 和标题编辑。
   - 新增 `EmptyDocuments.tsx`，负责空文档页。
   - 新增 `TreeSkeleton.tsx`，负责文档树加载态。
   - 新增 `queryKeys.ts`，统一 React Query cache key。

6. 补充中文注释。
   - 为 Auth 路由壳、启动恢复层、路由守卫、登录弹窗、营销首页、已登录工作区补充职责说明。
   - 为 Document 容器、侧边栏、树容器、树节点、顶部栏、详情页、空状态、query key 补充职责说明。
   - 注释重点说明“组件负责什么”和“为什么这样分层”，避免机械解释每一行。

## 验证结果

1. `pnpm typecheck` 通过。
2. `pnpm build:web` 通过。
3. IDE diagnostics 检查以下文件无 TypeScript 错误：
   - `AuthShell.tsx`
   - `AuthDialog.tsx`
   - `MarketingHome.tsx`
   - `DocumentWorkspace.tsx`
   - `WorkspaceSidebar.tsx`
   - `DocumentDetail.tsx`

## 下一步建议

1. 启动 `pnpm dev:web` 做浏览器视觉检查。
2. 继续按单文件单一职责规范微调后续新增组件。
3. 如果前端文件继续膨胀，优先拆分 container / presentational / hooks / types / queryKeys。
4. 后续进入 Editor MVP 前，先规划 editor、content API、autosave hook 的文件边界。
