# 2026-05-15 23:33:32 文档树交互提交前收口

## 已保留

- 恢复 BlockNote 默认 formatting toolbar，编辑器选中文本时可使用默认格式工具栏。
- 文档树操作对齐原 My-Notion：
  - hover 后显示 `MoreHorizontal` 更多菜单。
  - hover 后显示 `Plus` 添加子页面。
  - 重命名入口放入更多菜单。
- 新增 shadcn/Radix 风格基础组件：
  - `components/ui/dialog.tsx`
  - `components/ui/dropdown-menu.tsx`
- 新增 `RenameDocumentDialog.tsx`，替代原生 `window.prompt`。
- 更新 i18n 文案，覆盖更多操作、重命名弹窗、保存/取消等文本。
- 更新 Skills：
  - 禁止产品 UI 使用 `window.prompt/alert/confirm`。
  - 弹窗和菜单优先使用 shadcn/Radix 风格组件。
  - 文档树视觉交互继续对齐原 My-Notion。
- `Link` 保留 `draggable={false}`，避免拖动链接触发浏览器拆分视图。

## 暂缓

- 前端拖拽移动文档暂缓提交。
- 调试中确认过拖拽事件可以触发，拆分视图问题也已解决，但移动链路仍未达到可提交质量。
- 为避免提交不可用交互，已移除前端拖拽移动逻辑和相关样式。
- 后端 `PATCH /api/v1/documents/:id` 的 `parentId` 支持暂时保留，后续可基于更完整的拖拽方案继续接入。

## 清理

- 已停止 `document-drag-move` Debug Server。
- 已删除 `.dbg` 调试目录。
- 已删除 `debug-document-drag-move.md`。
- 已删除所有前端 debug 上报代码和 `debug-point` 标记。

## 验证

- `pnpm --filter @my-notion-go/web typecheck` 通过。
- `go test ./services/api/...` 通过。
- 全仓库搜索确认没有残留：
  - `document-drag-move`
  - `reportDocumentDragDebug`
  - `debug-point`
  - `127.0.0.1:7777`
