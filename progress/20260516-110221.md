# 2026-05-16 11:02:21 M4-02 AI Chat 前端最小闭环框架

## 背景

- 后端已完成 AI Chat 表结构、mock SSE 接口和 `smoke:api:ai-chat` 自动化验证。
- 本次进入前端最小闭环，不做完整产品细节，只先接通 UI、Hook 和 SSE parser。

## 实现

- `packages/api-client/src/index.ts` 新增 AI Chat 类型和 JSON API：
  - `AIConversation`
  - `AIMessage`
  - `aiChatApi.conversations`
  - `aiChatApi.createConversation`
  - `aiChatApi.messages`
- 新增 `apps/web/src/features/ai-chat/sse.ts`：
  - 支持增量解析 `text/event-stream`。
  - 处理 chunk 边界不完整的场景。
- 新增 `apps/web/src/features/ai-chat/api.ts`：
  - 使用原生 `fetch + ReadableStream` 调用 `/api/v1/ai/chat/stream`。
  - 将后端 SSE 事件规范化为前端类型。
- 新增 `apps/web/src/features/ai-chat/useAIChat.ts`：
  - 读取会话列表。
  - 读取当前会话消息。
  - 发送消息并维护流式 assistant 占位消息。
  - 支持停止当前流式请求。
- 新增 `apps/web/src/features/ai-chat/AIChatPanel.tsx`：
  - 右侧 AI 面板。
  - 会话列表。
  - 消息列表。
  - 输入框和发送/停止按钮。
- `DocumentNavbar.tsx` 新增 AI 入口。
- `DocumentWorkspace.tsx` 挂载右侧 `AIChatPanel`。
- `resources.ts` 新增 `zh/en` 两套 `aiChat` 文案。
- `global.css` 新增 AI 面板样式，保持 Notion-like 视觉。

## 验证

- `pnpm --filter @my-notion-go/web typecheck` 通过。
- `pnpm --filter @my-notion-go/web build` 通过。
- Vite build 仍有大 chunk 提示，来自现有 BlockNote/editor 依赖，不影响本次 AI Chat 接入。

## 后续

- 启动 Web + API 后做浏览器手动验证。
- 根据真实交互再补滚动到底、错误提示体验、会话标题更新和真实 LLM 接入。
