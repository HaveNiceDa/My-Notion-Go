# 2026-05-16 11:32:41 项目 Rules 本地化与 Global CSS Tailwind 迁移

## 背景

- 用户指出项目规则不应写入全局 memory，应按 Trae 官方项目规则机制放到项目目录。
- 用户同时要求继续清理 `global.css` 中的 feature 级全局样式，尽量迁移到组件调用位置的 Tailwind 写法。

## 规则处理

- 已撤销上次写入全局 memory 的新增规则。
- 新增项目规则：
  - `.trae/rules/frontend-engineering.md`
  - `.trae/rules/project-workflow.md`
- 规则采用 Trae 官方 frontmatter：
  - `alwaysApply: true`
- 前端规则覆盖：
  - Tailwind first。
  - shadcn/ui 本地组件优先。
  - i18n 强制。
  - ahooks 生命周期和稳定回调约定。
  - 新增代码关键注释要求。
  - 交付前检查 raw controls、硬编码文案、大段 global CSS 等。

## 样式迁移

- `MarketingHome` 从 `marketing-*` 全局 class 迁移到 Tailwind。
- `AuthDialog` 从 `auth-modal-logo`、`field-error`、`form-error`、`auth-switch-text` 迁移到 Tailwind。
- `LoadingScreen` 从 `loading-*` 迁移到 Tailwind。
- `WorkspaceSidebar` 从 `workspace-*`、`sidebar-*`、`user-*`、`document-section` 等迁移到 Tailwind。
- `DocumentNavbar`、`DocumentNavbarTitle`、`DocumentWorkspace` 迁移到 Tailwind。
- `DocumentTree`、`DocumentTreeItem`、`DocumentTreeActions` 迁移到 Tailwind，并将树操作按钮改为 `Button`。
- `DocumentDetail` 迁移到 Tailwind，并将标题输入框改为 `Input`。
- `DocumentEditor` 的容器/状态文案迁移到 Tailwind，保留 `.document-editor .bn-*` 作为 BlockNote 第三方覆盖作用域。
- `EmptyDocuments`、`TreeSkeleton`、`LanguageToggle` 迁移到 Tailwind。

## Global CSS 现状

- `global.css` 仅保留：
  - Tailwind directives。
  - design tokens 和 dark tokens。
  - reset/base。
  - logo dark/light 切换。
  - BlockNote `.bn-*` 第三方覆盖。
  - `blink` 动画。
  - 小屏 body overflow 修正。

## 验证

- 旧 feature class 残留检查通过。
- 业务组件中 raw `<button>`、`<input>`、`<textarea>` 已清理，只剩 `components/ui` primitive 内部封装。
- `pnpm --filter @my-notion-go/web typecheck` 通过。
- `pnpm --filter @my-notion-go/web build` 通过。
- IDE diagnostics 无新增问题。

## 注意

- Vite 仍提示大 chunk，来源仍是既有 BlockNote/editor 依赖，不属于本次样式迁移问题。
