# 2026-05-16 18:31 M5.3 RAG Chat SSE

## 背景

- M5.2 已完成 BlockNote JSONB 切块、DashScope embedding、`rag_chunks` 写入和 Qdrant upsert。
- 本阶段目标是把已索引知识库用于问答：query embedding、Qdrant search、RAG context prompt 和 `/api/v1/rag/chat/stream`。

## 实现内容

- 扩展 `internal/rag/qdrant.go`：
  - 新增 `SearchByUser`，使用当前用户 `userId` filter 限定检索边界。
  - 解析 Qdrant score、payload、point id，返回 `SearchResult`。
- 扩展 `internal/rag/service.go`：
  - 注入 `chat.Service`，复用 AI Chat 的会话、消息落库和 SSE streaming 能力。
  - 新增 query embedding + topK Qdrant search。
  - 构造 RAG system prompt，并限制上下文最大长度。
  - 将 citations 合并进 assistant message metadata。
- 扩展 `internal/rag/handler.go`：
  - 新增 `POST /api/v1/rag/chat/stream`。
  - SSE 事件复用 AI Chat 协议，并新增 `citations` 事件。
- 更新 `cmd/api/main.go`：
  - 先创建 `chatService`，再注入 `ragService`。
  - 注册 `/api/v1/rag/chat/stream`。
- 更新验证资产：
  - `scripts/smoke-rag-api.mjs` 覆盖 RAG SSE、citations 和 metadata。
  - `services/api/docs/rag.http` 补充 RAG Chat Stream 手动请求。
  - `milestones/M5-rag-worker-qdrant.md` 更新 M5.3 状态。

## 验证结果

- `node --check ./scripts/smoke-rag-api.mjs` 通过。
- `pnpm build:api` 通过。
- `go test ./services/api/...` 通过。
- `pnpm smoke:api:rag` 通过。
- `pnpm smoke:api:documents` 通过。
- `pnpm smoke:api:ai-chat` 通过。
- `pnpm smoke:api:embedding` 通过。

## 注意事项

- 本阶段仍是同步索引 + 同步检索，真实异步 job/worker 尚未接入。
- RAG search 目前按用户维度检索全部已索引文档，暂未支持单文档 scope 或 rerank。
- RAG 前端入口和引用展示留到 M5.4。
