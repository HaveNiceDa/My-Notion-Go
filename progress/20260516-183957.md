# 2026-05-16 18:39 M5.4 AI Panel RAG Mode

## 背景

- M5.3 已完成后端 `/api/v1/rag/chat/stream`，支持 query embedding、Qdrant search、RAG prompt 和 citations metadata。
- 本阶段先把后端 RAG Chat 接入现有 AI 面板，暂不展开完整知识库管理 UI。

## 实现内容

- 扩展 AI Chat 前端流式 API：
  - 普通模式调用 `/api/v1/ai/chat/stream`。
  - RAG 模式调用 `/api/v1/rag/chat/stream`。
  - SSE normalizer 兼容 `citations` 事件。
- 扩展 `useAIChat`：
  - 新增 `mode` 入参。
  - RAG streaming 时缓存 citations，并附加到临时 assistant message。
  - 最终 assistant message 继续使用后端落库后的 metadata。
- 更新 AI 面板：
  - 新增普通对话 / 知识库模式开关。
  - 使用 `localStorage` 持久化模式选择。
  - RAG 回复下方最小化展示最多 3 条引用摘要。
- 更新 i18n：
  - 补充中英文模式名称、模式说明和引用标题。
- 更新 M5 文档：
  - 记录 M5.4 前端集成已开始。

## 验证结果

- `pnpm typecheck` 通过。
- `pnpm build:web` 通过。
- `GetDiagnostics` 无错误。

## 后续

- 文档页面增加知识库状态入口。
- 支持开启、关闭、重建索引的前端操作。
- 引用展示后续可升级为可点击跳转到文档和 chunk 来源。
