# 2026-05-17 16:40 M5 收口验证

## 背景

- M5 已完成 RAG 最小闭环、异步索引 worker、AI 面板引用展示、引用来源定位与高亮。
- 会话过期 Modal 和 401 拦截代码已完成，用户确认先跳过人工验证，优先执行 M5 收口验证。

## 验证结果

- `pnpm --filter @my-notion-go/web build` 通过。
- `go test ./services/api/...` 通过。
- `API_BASE_URL=http://localhost:18080 pnpm smoke:api:rag` 通过：
  - 登录 smoke 用户。
  - 创建默认加入知识库的文档。
  - 写入 BlockNote 正文。
  - 等待索引完成。
  - 关闭知识库并确认 chunk 清理。
  - 重新开启知识库并等待异步索引完成。
  - 执行 RAG SSE 问答。
  - 验证 citations 包含来源文档和 `blockIds`。
  - 清理 RAG 索引和 smoke 文档。

## 备注

- 直接执行 `pnpm smoke:api:rag` 指向默认 `http://localhost:8080` 时，曾在 `blockIds` 断言处失败。
- 排查发现本地 `:8080` 上已有一个 `go run` 生成的 API 进程，疑似仍是 M5.7 前的旧代码响应结构。
- 为避免杀掉用户已有服务，本次临时在 `HTTP_ADDR=:18080` 启动当前工作区最新 API，并通过 `API_BASE_URL=http://localhost:18080` 完成收口验证。

## 结论

- M5 已收口完成。
- 下一阶段可进入 M6 产品基础能力，推荐从全局搜索/Command Palette 开始；也可以按已记录的 Agent + Tool 方向启动 Agent 模块设计。

